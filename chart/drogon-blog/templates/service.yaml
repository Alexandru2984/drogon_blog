apiVersion: v1
kind: Service
metadata:
  name: {{ include "drogon-blog.fullname" . }}
  labels: {{- include "drogon-blog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "drogon-blog.selectorLabels" . | nindent 4 }}
